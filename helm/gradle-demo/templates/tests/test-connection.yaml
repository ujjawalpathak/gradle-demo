apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gradle-demo.fullname" . }}-test-connection"
  labels:
    {{- include "gradle-demo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gradle-demo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
